# PRIOR FINDINGS — round 1 (same session, same working tree)

A five-agent review already ran over this diff earlier today and **all findings
below were fixed before you were spawned.** Every one has a regression test.

Your job is to find **NEW** issues. If you believe one of these is still
present, say so explicitly and mark it **PERSISTENT** with file:line proof —
do not re-report it as new. Do not spend turns re-verifying fixes.

## Round 1: security-reviewer (2 blockers, both fixed)

1. `jwks.ex` — the single-flight *waiter* read the cache with no
   `:stale_max_age` check, so when the lock winner correctly failed closed on a
   >24h-old key set, the concurrent waiter served those same keys and
   authenticated the token. Fixed: every stale read now routes through
   `serve_stale/3`. Test: `jwks_test.exs` "the single-flight waiter also
   honours :stale_max_age".
2. `sse.ex` — `:conduit_mcp_sse_connections` had no supervised Owner, so the
   table was owned by an SSE stream process and closing one connection reset
   `:active` to 0 while other streams were live, walking past
   `:max_connections`. Fixed: `ConduitMcp.Transport.SSE.Owner`, registered in
   `application.ex`.

## Round 1: elixir-idioms (1 P0, 2 P2, 3 P3 — all fixed)

- **P0** `Reflect.text/2` byte-clamped with `binary_slice/3` before a `/u`
  regex, so any multibyte value longer than `max*4` bytes left a truncated
  codepoint and `:re` raised `ArgumentError`. The handler's rescue reflects the
  method again and raised a second time from inside the rescue — no JSON-RPC
  response at all. Fixed with `String.replace_invalid("")`.
- `handle_notification/3` was a hand-written `case` while `methods/0` published
  `@notification_methods` — routed off the map now.
- Nothing pinned `@request_methods` against `route/5` clauses — test added.
- `component_scopes/2` / `build_scope_map/2` lost the map's accidental
  de-duplication → duplicate clause heads + "cannot match" warning against
  generated code. `Enum.uniq_by/2` added.
- `Tasks.list(limit: :infinity)` returned `[]` — clause added.
- `Principal.put/2` did not normalise `:id`, so a non-scalar id violated
  `id/1`'s `@spec` and made `rate_limit_key/1` raise in-pipeline.

## Round 1: otp-concurrency (1 P1, 5 warnings — all fixed)

- **P1** the SSE keep-alive catch-all drained Bandit's
  `{:bandit, {:send_window_update, _}}` and `{:bandit, {:rst_stream, _}}` out of
  the stream process that reads them back. Guarded now.
- The five `Owner` Agents raised on a taken table name → three restarts in five
  seconds takes down `ConduitMcp.Supervisor`. All now share
  `ConduitMcp.EtsOwner`, which logs and degrades.
- The cancellation janitor emitted `[:conduit_mcp, :session, :cleanup]` and
  double-published the count → `:telemetry_event` / `:noun` options added.
- `function_exported?/3` without `Code.ensure_loaded?/1` silently idles a
  janitor forever under interactive code loading.
- `:session_janitor` / `:cancellation_janitor` raised `CaseClauseError` on
  `true`.
- The default janitor's 30-min TTL overrides a longer per-transport
  `session: [ttl: ...]` — documented at `application.ex:72-80`, not changed.

## Round 1: test-quality (10 findings — all fixed)

Tautological idempotence property; manual-mode `-32002` pinned nowhere; two
`refute errors == []`; two bare-401 OAuth assertions; `async: true` module
wiping the global cancellation table; global SSE counter assertions;
sleep-then-check mailbox drain + bare `:DOWN` atom; implicit
`refute_receive_timeout`; `phash2` handler ids; stale async-invariant comments.

## Explicitly rejected in round 1 — do not re-raise without new evidence

Gating the JWKS **TTL-miss** path on `cooling_down?/2`. It would let the
cooldown override the operator's explicit `:cache_ttl`, and checking it before
the lock preempts the single-flight wait: a request arriving after the winner
recorded `:last_refresh` but before it published keys would fail instead of
waiting. The pile-up it guards against is already bounded — one process
fetches, the rest wait, and the winner releases the lock as soon as it
finishes. Rationale is recorded at `jwks.ex:131-140`.

## Accepted residuals — documented in code, not defects

- With neither sessions nor auth, the cancellation scope degrades to the client
  IP, so clients behind one NAT share a namespace (`cancellation.ex:41-49`).
- `:tasks_require_owner` defaults to false; an unstamped task stays
  world-readable. Deliberate, so the public API stays stable
  (`ConduitMcp.Tasks.get/2` moduledoc).
- RC9 uses `-32602`/`-32002` (MCP spec) rather than the plan's `-32601`.
  Recorded as a deviation in `CHANGELOG.md`.
